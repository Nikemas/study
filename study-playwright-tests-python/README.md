# 🎭 Playwright Tests (Python) - Educational AI Platform

Автоматизированные end-to-end тесты для образовательной AI-платформы с использованием **Python + Playwright + pytest**.

![Playwright](https://img.shields.io/badge/Playwright-1.40.0-45ba4b?style=flat&logo=playwright&logoColor=white)
![Python](https://img.shields.io/badge/Python-3.8+-3776AB?style=flat&logo=python&logoColor=white)
![pytest](https://img.shields.io/badge/pytest-7.4.3-0A9EDC?style=flat&logo=pytest&logoColor=white)

## 📁 Структура проекта

```
study-playwright-tests-python/
├── pages/                     # Page Object Model
│   ├── __init__.py
│   ├── main_page.py          # Главная страница и навигация
│   ├── chat_page.py          # Функционал AI-чата
│   └── knowledge_page.py     # База знаний
├── tests/                     # Тестовые сценарии
│   ├── __init__.py
│   ├── test_smoke.py         # Smoke тесты (6 тестов)
│   ├── test_chat.py          # Тесты чата (8 тестов)
│   └── test_knowledge.py     # Тесты базы знаний (10 тестов)
├── reports/                   # HTML отчёты (создаётся автоматически)
├── conftest.py               # Pytest фикстуры и хуки
├── pytest.ini                # Конфигурация pytest
├── requirements.txt          # Python зависимости
└── README.md                # Эта документация
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создать виртуальное окружение (рекомендуется)
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Установить браузеры для Playwright
playwright install
```

### 2. Запуск приложения

Перед запуском тестов убедись, что приложение запущено:

```bash
# В папке study (основного проекта)
npm start
```

Приложение должно быть доступно по адресу `http://localhost:3000`

### 3. Запуск тестов

```bash
# Запуск всех тестов
pytest

# Запуск с видимым браузером
pytest --headed

# Запуск в slow-mo режиме (замедленно)
pytest --headed --slowmo=1000

# Запуск конкретного файла
pytest tests/test_smoke.py

# Запуск с подробным выводом
pytest -v

# Запуск в несколько потоков (быстрее)
pytest -n 3
```

## 📊 Типы тестов

### 🔥 Smoke Tests (`test_smoke.py`)
**Маркер:** `@pytest.mark.smoke`

Быстрые проверки критичного функционала:
- Загрузка приложения
- Навигация между вкладками
- Переключение темы
- Наличие основных элементов

**Время выполнения:** ~10-15 секунд
**Количество тестов:** 6

### 💬 Chat Tests (`test_chat.py`)
**Маркер:** `@pytest.mark.chat`

Тестирование AI-чата:
- Отправка сообщений
- Получение ответов от AI
- Валидация пустых сообщений
- Работа с длинными сообщениями
- Контекстное понимание
- Специальные символы

**Время выполнения:** ~30-60 секунд (зависит от API)
**Количество тестов:** 8

**⚠️ Важно:** Для работы AI-тестов нужен настроенный Groq API ключ!

### 📚 Knowledge Tests (`test_knowledge.py`)
**Маркер:** `@pytest.mark.knowledge`

Тестирование базы знаний:
- Просмотр материалов по всем темам (Python, JS, HTML, CSS, React)
- Наличие примеров кода
- Подсветка синтаксиса
- Переключение между темами
- Параметризованные тесты

**Время выполнения:** ~20-30 секунд
**Количество тестов:** 10

## 🎯 Запуск по маркерам

```bash
# Только smoke тесты
pytest -m smoke

# Только тесты чата
pytest -m chat

# Только тесты базы знаний
pytest -m knowledge

# Только быстрые тесты (исключая медленные)
pytest -m "not slow"

# Smoke + Knowledge (исключая chat)
pytest -m "smoke or knowledge"
```

## 🎨 Page Object Model (POM)

Тесты используют паттерн Page Object Model для лучшей поддерживаемости.

### Пример использования:

```python
from playwright.sync_api import Page
from pages import MainPage, ChatPage

def test_example(page: Page):
    # Создаём объекты страниц
    main_page = MainPage(page)
    chat_page = ChatPage(page)
    
    # Используем методы страниц
    main_page.goto()
    main_page.open_chat()
    chat_page.send_message("Привет!")
    chat_page.wait_for_ai_response()
    
    # Проверки
    chat_page.expect_ai_response_not_empty()
```

### MainPage (main_page.py)
Управление навигацией и темой:
```python
main_page = MainPage(page)
main_page.goto()
main_page.open_chat()
main_page.toggle_theme()
theme = main_page.get_current_theme()
```

### ChatPage (chat_page.py)
Работа с AI-чатом:
```python
chat_page = ChatPage(page)
chat_page.send_message("Что такое Python?")
chat_page.wait_for_ai_response()
response = chat_page.get_last_ai_message()
```

### KnowledgePage (knowledge_page.py)
Просмотр учебных материалов:
```python
knowledge_page = KnowledgePage(page)
knowledge_page.select_python()
has_code = knowledge_page.has_code_examples()
```

## ⚙️ Конфигурация

### pytest.ini
Основные настройки в `pytest.ini`:
- Браузер: Chromium (можно изменить на firefox/webkit)
- Headless режим: по умолчанию включён
- Видео: сохраняется при падении
- Скриншоты: делаются при падении
- HTML отчёт: генерируется автоматически

### conftest.py
Фикстуры и хуки:
- `browser_context_args` - настройка браузера
- `page` - фикстура страницы с таймаутами
- `base_url` - базовый URL приложения
- `before_each` - выполняется перед каждым тестом

### Изменить настройки:

**1. Изменить браузер:**
```bash
pytest --browser=firefox
pytest --browser=webkit
```

**2. Включить headed режим:**
```bash
pytest --headed
```

**3. Изменить базовый URL:**
Отредактируй в `conftest.py`:
```python
BASE_URL = "http://your-url:port"
```

## 📸 Отчёты и артефакты

### HTML Отчёт
После запуска тестов автоматически генерируется отчёт:
```bash
# Открыть отчёт
open reports/report.html  # macOS
xdg-open reports/report.html  # Linux
start reports/report.html  # Windows
```

### Артефакты
При падении тестов сохраняются:
- 📸 Скриншоты
- 🎥 Видео (если включено)
- 📝 Traces для отладки

Находятся в директории `test-results/`

## 🐛 Отладка

### 1. Debug режим
```bash
pytest --headed --slowmo=1000 -v
```

### 2. Запуск одного теста
```bash
pytest tests/test_smoke.py::TestSmoke::test_application_loads_successfully -v
```

### 3. Остановка на первом падении
```bash
pytest -x
```

### 4. Запуск упавших тестов
```bash
pytest --lf  # last failed
```

### 5. Playwright Inspector
```bash
PWDEBUG=1 pytest tests/test_smoke.py
```

### 6. Print debugging
```python
def test_example(page: Page):
    print(f"Current URL: {page.url}")  # Выведется в консоль с -s
```

Запуск с выводом print:
```bash
pytest -s
```

## 📝 Написание новых тестов

### Базовый шаблон:

```python
import pytest
from playwright.sync_api import Page
from pages import MainPage

@pytest.mark.smoke
class TestMyFeature:
    """Описание группы тестов"""
    
    @pytest.fixture(autouse=True)
    def setup(self, page: Page):
        """Выполняется перед каждым тестом"""
        main_page = MainPage(page)
        main_page.goto()
    
    def test_my_feature(self, page: Page):
        """Описание теста"""
        # Arrange (подготовка)
        main_page = MainPage(page)
        
        # Act (действие)
        main_page.open_chat()
        
        # Assert (проверка)
        main_page.expect_chat_tab_visible()
```

### Лучшие практики:

1. **Используй POM** - не пиши локаторы в тестах
2. **Один тест = одна проверка** - тесты должны быть фокусными
3. **Говорящие имена** - `test_user_can_send_message`
4. **Используй фикстуры** - для переиспользования кода
5. **Маркируй тесты** - `@pytest.mark.smoke`, `@pytest.mark.slow`
6. **Документируй** - добавляй docstrings к тестам

### Параметризация:

```python
@pytest.mark.parametrize("topic,keyword", [
    ("Python", "переменная"),
    ("JavaScript", "const"),
    ("HTML", "тег"),
])
def test_knowledge_content(page: Page, topic, keyword):
    knowledge_page = KnowledgePage(page)
    getattr(knowledge_page, f"select_{topic.lower()}")()
    content = knowledge_page.get_content_text()
    assert keyword in content.lower()
```

## ⚠️ Известные проблемы

### 1. API ключ не настроен
**Проблема:** Тесты чата падают с ошибкой API  
**Решение:** Настрой `.env` файл в основном проекте study с ключом Groq

### 2. Приложение не запущено
**Проблема:** Тесты не могут подключиться к localhost:3000  
**Решение:** Запусти `npm start` в папке study перед тестами

### 3. Playwright не установлен
**Проблема:** `playwright: command not found`  
**Решение:** 
```bash
pip install playwright
playwright install
```

### 4. Медленные тесты чата
**Проблема:** Тесты с AI работают долго  
**Решение:** Это нормально - AI требует времени. Используй маркер `-m "not slow"` для быстрых прогонов

### 5. Timeout ошибки
**Проблема:** Тесты падают по таймауту  
**Решение:** Увеличь таймауты в `conftest.py`:
```python
page.set_default_timeout(20000)  # 20 секунд
```

## 🔍 CI/CD Integration

### GitHub Actions

Создай `.github/workflows/tests.yml`:

```yaml
name: Playwright Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install --with-deps
      
      - name: Run tests
        run: pytest --html=reports/report.html
        
      - name: Upload test results
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: playwright-report
          path: reports/
```

## 📚 Дополнительные команды

### Полезные pytest команды:

```bash
# Показать все доступные тесты
pytest --collect-only

# Запуск с покрытием кода (нужен pytest-cov)
pytest --cov=pages --cov-report=html

# Параллельный запуск (нужен pytest-xdist)
pytest -n auto

# Генерация allure отчёта (нужен allure-pytest)
pytest --alluredir=./allure-results
allure serve ./allure-results

# Профилирование тестов
pytest --durations=10  # показать 10 самых медленных тестов
```

## 📊 Метрики покрытия

Текущее тестовое покрытие:

| Функционал | Покрытие | Тестов |
|-----------|----------|--------|
| Навигация | 100% | 6 |
| Тема | 100% | 2 |
| Чат (базовый) | 90% | 8 |
| База знаний | 100% | 10 |
| История | 0% | 0 (TODO) |
| Оценка ответов | 0% | 0 (TODO) |

**Всего тестов:** 24

## 🤝 Вклад в проект

При добавлении новых фич:
1. Обнови Page Objects в `pages/`
2. Добавь соответствующие тесты в `tests/`
3. Добавь маркеры если нужно
4. Запусти все тесты локально
5. Обнови документацию

## 👨‍💻 Автор

**Ardak Aibekuulu**
- Telegram: @Nikemass
- GitHub: Nikemas
- Email: aibekuuluardak@gmail.com

---

## 🎓 Для изучения Playwright

Этот проект создан как демонстрация навыков автоматизации тестирования.

**Что здесь показано:**
✅ Page Object Model паттерн  
✅ pytest фикстуры и хуки  
✅ Маркировка и параметризация тестов  
✅ Работа с async элементами (AI ответы)  
✅ HTML отчёты  
✅ Правильная структура проекта  
✅ Документация и комментарии  

**Технологии:**
- Python 3.8+
- Playwright 1.40+
- pytest 7.4+
- Page Object Model
- pytest-playwright plugin

---

**💡 Совет:** Все Page Objects и тесты содержат подробные docstrings и комментарии. Читай код - там всё объяснено! 

**🚀 Удачи в изучении Playwright!**
